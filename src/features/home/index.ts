/**
 * Home Feature — public API
 *
 * Exports the feature's views, composables, and store.
 * Other features should import from here, not from internal paths.
 */

// Views
export { default as HomeView } from './views/HomeView.vue'
