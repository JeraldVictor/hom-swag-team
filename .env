# Default environment variables (shared baseline, committed to git)
# Override these in .env.local (local dev) or .env.prod (production build)

VITE_BFF_API_URL=http://localhost:3000/bff
VITE_GOOGLE_MAPS_API_KEY=
